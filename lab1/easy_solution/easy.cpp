#include <iostream>

using namespace std;

int main(){
    system("lspci -nn");
    cout << "Actually, I could read all data from files in Linux, but it wouldn't be interesting, so...\neasy peasy lemon squeezy";
    return 0;
}